package app

/*
It is advised to keep the application config in application layer. Config
parsing and initialization should be passed in application layer.

You can separate the config into different config files for each application.
*/
